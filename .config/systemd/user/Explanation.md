Use this folder to load systemd services to, for example, launch on startup.\
They need to be in a `{name}.service` format. 

Also, make sure to use `systemctl --user` when interacting with these services. 
